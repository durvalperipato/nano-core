name: nano_core_example
description: "A professional showcase example for nano_core architecture framework."
publish_to: 'none'
version: 0.0.5-dev

environment:
  sdk: ^3.10.3
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  get_it: ^9.2.0
  intl: any
  nano_core:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  generate: true
  uses-material-design: true
