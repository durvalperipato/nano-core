/// Dart plugin entrypoint for Flutter platforms using `dartPluginClass`.
class NanoCorePlugin {
  /// Registers the plugin for multiplatform Dart execution.
  static void registerWith([dynamic registrar]) {
    // No-op: Platform channel calls on Web/Desktop are handled gracefully.
  }
}
