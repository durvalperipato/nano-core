/// Standard default paths used across nano-core routing.
abstract final class NanoPaths {
  /// The root base path (`'/'`).
  static const String root = '/';

  /// Default sub-route detail path (`'/detail'`).
  static const String detail = '/detail';
}
