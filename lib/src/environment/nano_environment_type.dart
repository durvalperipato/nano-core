/// Represents standard Flutter compilation and execution modes.
enum NanoEnvironmentType {
  /// Development environment (Debug mode).
  development,

  /// Profile mode (Performance testing / profiling).
  profile,

  /// Production environment (Release mode).
  production,
}
