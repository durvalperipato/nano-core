name: nano_core
description: "A lightweight reactive architecture framework and design system toolkit for Flutter multiplatform applications."
version: 0.6.0
homepage: https://nanodevs.com.br
repository: https://github.com/durvalperipato/nano-core
topics:
  - architecture
  - state-management
  - design-system
  - routing
  - scaffold

environment:
  sdk: ^3.10.3
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  get_it: ^9.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
